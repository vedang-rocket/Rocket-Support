---
description: >
  StripeSignatureVerificationError no signatures found webhook fails, StripeInvalidRequestError
  no such price, no such customer cus_, Stripe webhook signature verification failed,
  Netlify build fails TypeScript error, ENOENT no such file module not found build,
  CORS error calling external API, emails not sending production Resend, build command failed,
  next build TypeScript error, environment variable undefined in production netlify,
  SPA routing 404 on page refresh, image optimization error Invalid src prop hostname,
  API route 404 production not local, stripe test vs live key wrong environment,
  missing stripe webhook secret, checkout session creation failed
globs: ["**/*.ts", "**/*.tsx", "**/*.js"]
alwaysApply: false
---

## ⚡ DEPLOYMENT / BUILD ERRORS

### Netlify build fails: `Error: Cannot find module 'X'`
**Cause**: Package missing from `package.json` or not installed
```bash
# ✅ FIX locally first
npm install missing-package
# Commit package.json and package-lock.json
# Netlify uses package.json to install dependencies
```

---

### Netlify build fails: TypeScript errors
**Cause**: Next.js treats TS errors as build failures in production
```bash
# ✅ FIX — find all TS errors
npx tsc --noEmit

# Quick fix if you need to unblock the build (not ideal long-term):
# In next.config.ts:
const nextConfig = {
  typescript: { ignoreBuildErrors: true }  // temporary only
}
```

---

### App works in preview but broken on Netlify
**Cause**: Environment variables not set in Netlify
```
✅ FIX:
Netlify Dashboard → your site → Site Configuration → Environment Variables
Add ALL variables from .env.local:
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET
- STRIPE_PRICE_ID
- NEXT_PUBLIC_SITE_URL (set to https://your-app.netlify.app)
- RESEND_API_KEY (if using email)

Then: Deploys → Trigger deploy → Clear cache and deploy site
```

---

### `CORS error` when calling external API from browser
**Cause**: External API doesn't allow browser (cross-origin) requests
```typescript
// ✅ FIX — create a Next.js API route as proxy
// app/api/proxy/route.ts
import { NextResponse } from 'next/server'

export async function POST(request: Request) {
  const body = await request.json()
  
  const response = await fetch('https://external-api.com/endpoint', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${process.env.EXTERNAL_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(body),
  })
  
  const data = await response.json()
  return NextResponse.json(data)
}

// Frontend calls /api/proxy instead of the external URL directly
```

---

### Emails not sending in production
**Cause**: Email API key for production not set, OR sending from unverified domain
```typescript
// ✅ FIX — verify domain in Resend/SendGrid dashboard
// "From" address MUST use a verified domain:
// ✅ from: 'hello@yourdomain.com'
// ❌ from: 'hello@gmail.com'  (blocked by email providers)

// Check RESEND_API_KEY is set in Netlify env vars
// Test in production by checking email provider dashboard logs
```
